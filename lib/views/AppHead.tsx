import { Header, Heading } from "grommet";

 const AppHead = () => (
  <Header background="brand" pad={{vertical: '0å', horizontal: 'medium'}}>
    <Heading level={1}>Site Head</Heading>
  </Header>
)

export default AppHead;
